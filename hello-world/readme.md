# hello-world

### Compile the Rust Program
```bash
rustc main.rs

# with cargo
# (compiles and run)
cargo run main.rs 
# or 
# (only compiles)
cargo build 
```

### Run the Compiled Program
```bash
./main.exe
```